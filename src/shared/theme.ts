import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: { main: '#009382' },
        secondary: { main: '#FFA17A' },
        warning: { main: '#F31431' },
    },
});

export default theme;
